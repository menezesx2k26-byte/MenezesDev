# O fluxo raster oficial usa os skills repo-local
# `$menezesdev-image-director` e `$imagegen` nativo do Codex.
# Nenhum servidor MCP de imagem ou variável de API é necessário.

# Configurações MCP não relacionadas podem ser adicionadas abaixo quando aprovadas.
